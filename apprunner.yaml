version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  pre-run:
    - pip3 install -r requirements.txt
  command: python3 -m streamlit run dashboard/app.py
  network:
    port: 8080
